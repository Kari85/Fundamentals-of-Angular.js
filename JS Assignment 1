/*
  Given an array of numbers, return the sum of all the numbers in the array.
  For example:
  arraySum([5, 5, 1, 3]) == 14
*/
const arraySum = (arr) => {
  let sum = 0;
  for (let i = 0; i < arr.length; i++) {
    sum += arr[i];
  }
  return sum;
};
